只有一个方法的接口称为功能性接口

如：
	interface Foo {
		int test(int a, int b);
	}

利用Lambda 实现功能性接口

语法：
	Foo f1 = (int a, int b) -> { return a + b; };

特点：
	1. 基本语法 () -> {}
	2. 如果Java 编译器能够猜到参数类型，则类型可以省略。
		Foo f1 = (a, b) -> { return a + b; };
	3. 如果只有一个参数，则() 可以省略
		Goo g1 = a -> { return a + 1; };
	4. 如果{} 中只有一行语句，则{} 可以省，如果是return 语句，则一起省略!
		Goo g1 = a -> a + 1;
		





















































































































































































